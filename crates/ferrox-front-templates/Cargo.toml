[package]
description = "Production-ready enterprise SaaS page templates (Auth pages, Pricing tables, Admin Dashboard layouts)."
name = "ferrox-front-templates"
version = "0.1.0"
edition = "2021"

[dependencies]
ferrox-front-core = { path = "../ferrox-front-core" }
ferrox-front-ui = { path = "../ferrox-front-ui" }
ferrox-front-macro = { path = "../ferrox-front-macro" }
web-sys = { version = "0.3", features = ["console"] }
